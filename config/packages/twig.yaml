twig:
    default_path: '%kernel.project_dir%/templates'
    debug: '%kernel.debug%'
    strict_variables: '%kernel.debug%'
    form_themes:
        - 'bootstrap_4_layout.html.twig'
    globals:
        image_article_thumbnail_directory: '%image_articles_thumbnails_directory%'
        image_article_thumbnail_display: '%image_articles_thumbnails_display%'
